<section>  
  <div class="page-header">
    <h1><%= singular_table_name.humanize %></h1>
  </div>

   <div class="well">
    <div class="btn-group">
      <%%= link_to icon_tag('Editar', :class => 'icon-edit icon-white'), edit_<%= singular_table_name %>_path(@<%= singular_table_name %>), :class => 'btn btn-primary' %>
      <%%= link_to icon_tag('Borrar', :class => 'icon-trash icon-white'), @<%= singular_table_name %>, confirm: 'Estás seguro?', method: :delete, class: 'btn btn-primary' %>
      <%%= link_to icon_tag('Ver Todos', :class => 'icon-arrow-left icon-white'), <%= index_helper %>_path, class: 'btn btn-primary' %>
    </div>
  </div>

  <% attributes.each do |attribute| -%>
    <p>
      <b><%= attribute.human_name %>:</b>
      <%%= @<%= singular_table_name %>.<%= attribute.name %> %>
    </p>
    
  <% end -%>
</section>
