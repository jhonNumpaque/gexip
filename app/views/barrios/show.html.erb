<section>  
  <div class="page-header">
    <h1>Barrio</h1>
  </div>

   <div class="well">
    <div class="btn-group">
      <%= link_to icon_tag('Editar', :class => 'icon-edit icon-white'), edit_barrio_path(@barrio), :class => 'btn btn-primary' %>
      <%= link_to icon_tag('Borrar', :class => 'icon-trash icon-white'), @barrio, confirm: 'Estás seguro?', method: :delete, class: 'btn btn-primary' %>
      <%= link_to icon_tag('Ver Todos', :class => 'icon-arrow-left icon-white'), barrios_path, class: 'btn btn-primary' %>
    </div>
  </div>

      <p>
      <b>Nombre:</b>
      <%= @barrio.nombre %>
    </p>
    
      <p>
      <b>Type:</b>
      <%= @barrio.type %>
    </p>
    
      <p>
      <b>Territorio:</b>
      <%= @barrio.territorio_id %>
    </p>
    
  </section>
