<section>
  <div class="alert-message notice" style="margin-top:20px;">    
    <p><%= notice %></p>
  </div> 

  <div class="page-header">
    <h1>pais</h1>
  </div>

   <div class="well">
    <%= link_to 'Editar', edit_pais_path(@pais), :class => 'btn btn-primary' %>
    <%= link_to 'Borrar', @pais, confirm: 'Estás seguro?', method: :delete, class: 'btn btn-primary' %>
    <%= link_to 'Ver Todos', paises_path, class: 'btn btn-primary' %>
  </div>

      <p>
      <b>Nombre:</b>
      <%= @pais.nombre %>
    </p>

      <p>
      <b>Tipo:</b>
      <%= @pais.type %>
    </p>
<!--
      <p>
      <b>Territorio:</b>
      <%= @pais.territorio_id %>
    </p>
-->
  </section>
