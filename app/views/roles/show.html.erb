<section>

  <div class="page-header">
    <h1>Rol "<%= @rol.nombre  %>" </h1>
  </div>

  <div class="well">
    <div class="btn-group">
      <%= link_to icon_tag('Editar', :class => 'icon-edit icon-white'), edit_rol_path(@rol), :class => 'btn btn-primary' %>
      <%= link_to icon_tag('Borrar', :class => 'icon-trash icon-white'), @rol, confirm: 'Estás seguro?', method: :delete, class: 'btn btn-primary' %>
      <%= link_to icon_tag('Ver Todos', :class => 'icon-arrow-left icon-white'), roles_path, class: 'btn btn-primary' %>
    </div>
  </div>


</section>
