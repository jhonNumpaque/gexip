<section>
  <div class="alert-message notice" style="margin-top:20px;">    
    <p><%= notice %></p>
  </div> 

  <div class="page-header">
    <h1>accion</h1>
  </div>

   <div class="well">
    <%= link_to 'Editar', edit_accion_path(@accion), :class => 'btn primary' %>
    <%= link_to 'Borrar', @accion, confirm: 'Estás seguro?', method: :delete, class: 'btn' %>
    <%= link_to 'Ver Todos', acciones_path, class: 'btn' %>
  </div>

      <p>
      <b>Accion:</b>
      <%= @accion.accion %>
    </p>

  </section>
