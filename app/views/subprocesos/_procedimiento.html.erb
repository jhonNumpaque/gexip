<td><%= procedimiento.nombre %></td>
<td><%= format_date(procedimiento.updated_at) %></td>
<td>
	<div class="pull-right">
		<%= link_to icon_tag('', :class => 'icon-trash'), procedimiento_path(procedimiento), 
		:title => 'Eliminar Procedimiento', :method => :delete, :remote => true,
		:confirm => "¿Está seguro de eliminar este procemiento?.\nEsta acción no se puede revertir" %> 
		<%= link_to icon_tag('', :class => 'icon-pencil'), 
		edit_procedimiento_path(id: procedimiento, from: 'tree'), remote: true %>
	</div>
</td>