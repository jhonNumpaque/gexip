<section>
  <div class="page-header">
    <h1>Controles <small>Lista de controles</small></h1>
  </div>

  <div class="well">
    <%= link_to 'Agregar', new_control_path, :class => 'btn btn-primary' %>
  </div>
  <br />

  <% if @controles.present? %>
    <table class="table table-striped table-bordered table-condensed">
      <tr>
              <th>Nombre</th>
              
        <th></th>
      </tr>
      <% @controles.each do |control| %>
        <tr>
                      <td><%= control.nombre %></td>
                    <td>
            
            <div class="btn-group">              
              <%= link_to 'Acciones <b class="caret"></b>'.html_safe, '#', :class => 'btn btn-primary dropdown-toggle', 'data-toggle' => 'dropdown' %>
              <ul id="menu1" class="dropdown-menu">
                <li><%= link_to icon_tag('Editar', :class => 'icon-pencil'), edit_control_path(control) %></li>
                <li><%= link_to icon_tag('Eliminar', :class => 'icon-trash'), control, confirm: '¿Está seguro de eliminar este registro?. Esta acción no se puede deshacer', method: :delete %></li>

              </ul>              
            </div>
          </td>
        </tr>
      <% end %>
    </table> 
  <% else %>
    <p class="alert-message warning">
      Todavía no hay controles, <%= link_to 'agregar', new_control_path %>.
    </p>
  <% end %>

</section>
